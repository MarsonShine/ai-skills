interface:
  display_name: "Reviewable Change Slices"
  short_description: "按明确要求逐个交付可审查的代码切片并设置人工检查点"
  default_prompt: "Use $reviewable-change-slices to plan this work as commit-sized slices and pause at the requested human review checkpoints."
