# Saved Image Preferences

Use this reference only when the user asks to save or change defaults. A missing `EXTEND.md` does not block prompt preparation or generation.

Read `extend-schema.md` for supported fields. Reuse values already supplied; ask together only for unresolved persistent choices, including project versus user scope when it is not established.

- Project: `.baoyu-skills/qwen-image-generator/EXTEND.md`
- User: `$XDG_CONFIG_HOME/baoyu-skills/qwen-image-generator/EXTEND.md` when configured, otherwise `$HOME/.baoyu-skills/qwen-image-generator/EXTEND.md`

Update only requested fields and preserve unrelated existing preferences. Save within the authorized scope, report the path, and continue any pending image task. If persistence is blocked, the image task can still use the resolved settings without writing preferences.
