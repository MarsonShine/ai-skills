interface:
  display_name: "Loop Orchestrator"
  short_description: "为定时、持续监控、事件处理和重复达标任务定义执行循环边界"
  default_prompt: "Use $loop-orchestrator to define the trigger, stop condition, validation, and cost boundary for this recurring or repeated-attempt workflow."
