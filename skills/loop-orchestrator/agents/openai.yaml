interface:
  display_name: "Loop Orchestrator"
  short_description: "复杂任务循环编排、停止条件、验证策略与权限执行边界"
  default_prompt: "Use $loop-orchestrator to classify this complex task, choose the right loop, and execute or plan based on permissions."
